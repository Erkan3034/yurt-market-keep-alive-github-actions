name: Keep Render Backend Alive

on:
  schedule:
    - cron: "*/12 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render backend
        run: |
          echo "Pinging backend..."
          curl -m 10 -s -o /dev/null -w "%{http_code}" \
          https://yurt-market-api.onrender.com/api/keep-alive/
